// const bcrypt = require("bcryptjs");

// async function ComparePasswords(plainPassword: any, hashedPassword: any) {
//   return await bcrypt.compare(plainPassword, hashedPassword);
// }

// export default ComparePasswords;
